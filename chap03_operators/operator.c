#include<stdio.h>
 
int main() {
    int a, b;
    printf("\nEnter the 1st Value:");
    scanf("%d", &a);
    printf("\nEnter the 2nd Value:");
    scanf("%d", &b);
    printf("\nSum of Two value is:%d", (a + b));
    printf("\nDifference between Two Values is:%d", (a - b));
    printf("\nProduct of two number is:%d", (a * b));
    return 0;
}
